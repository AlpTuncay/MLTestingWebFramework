import React from "react"

const axios = require("axios").default;

class ModelInfo extends React.Component{

  constructor(props){
    super(props)
  }

  fetchModelInformation = () => {
    //Send the request to endpoint /model/info/{modelId} to fetch model information
  }

  
}
